name: CI - Build, Push to GAR, Update GitOps Repo

on:
  push:
    branches: [ main ]

env:
  REGION: asia-south1
  IMAGE_NAME: backend-app

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ✅ Step 1: Checkout your application source code
      - name: Checkout Code
        uses: actions/checkout@v4

      # ✅ Step 2: Setup GCP SDK and authenticate with your service account
      - name: Setup Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}
          service_account_key: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}

      # ✅ Step 3: Verify authentication and project context
      - name: Verify GCP Auth
        run: |
          echo "🔍 Verifying GCP authentication..."
          gcloud auth list
          gcloud config list project

      # ✅ Step 4: Configure Docker for Artifact Registry
      - name: Configure Docker for GAR
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      # ✅ Step 5: Build and Push Docker image (fixed activation)
      - name: Build and Push Image
        run: |
          IMAGE_TAG=${GITHUB_RUN_NUMBER}
          IMAGE_URI=${{ env.REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/backend-repo/${{ env.IMAGE_NAME }}:${IMAGE_TAG}

          echo "🚀 Building Docker image: $IMAGE_URI"
          docker build -t $IMAGE_URI .

          echo "🔐 Activating service account..."
          echo '${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}' > ${HOME}/gcp-key.json
          gcloud auth activate-service-account --key-file=${HOME}/gcp-key.json
          gcloud config set project ${{ secrets.GCP_PROJECT_ID }}

          echo "📦 Authenticating Docker to GAR..."
          gcloud auth print-access-token | docker login -u oauth2accesstoken --password-stdin https://${{ env.REGION }}-docker.pkg.dev

          echo "⬆️ Pushing image to Artifact Registry..."
          docker push $IMAGE_URI

          echo "IMAGE_URI=$IMAGE_URI" >> $GITHUB_ENV

      # ✅ Step 6: Clone your GitOps repository
      - name: Clone GitOps Repo
        uses: actions/checkout@v4
        with:
          repository: dileep123321/backend-deploy-repo
          token: ${{ secrets.GITOPS_REPO_TOKEN }}
          path: deploy

      # ✅ Step 7: Update manifest
      - name: Update Kubernetes Manifest
        run: |
          cd deploy/manifests
          echo "🔧 Updating image tag in deployment.yaml..."
          sed -i "s|image: .*|image: $IMAGE_URI|" deployment.yaml
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Update image to $IMAGE_URI" || echo "No changes to commit"
          git push

      # ✅ Step 8: Completed
      - name: Completed
        run: echo "✅ Image pushed to GAR and manifests updated successfully!"
